services:
  caddy:
    ports: !override
      - "127.0.0.1:80:80"

    volumes: !override
      - ./Caddyfile.funnel:/etc/caddy/Caddyfile:ro
      - portfolio_caddy_data:/data
      - portfolio_caddy_config:/config